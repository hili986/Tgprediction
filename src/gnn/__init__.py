"""GNN sub-package for Physics-Embedded Graph Neural Networks.

Modules:
    graph_builder  — Polymer SMILES -> PyG Data graph conversion
    physics_gat    — 3-layer GAT + GRIN repeat-invariant pooling
    tandem_m2m     — Tandem M2M: GNN + tabular fusion architecture
    pretrainer     — Two-stage training (pretrain -> finetune)
    multitask      — Multi-task GNN with shared backbone
    ensemble       — Deep ensemble + conformal prediction
"""
