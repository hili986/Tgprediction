# 论文写作交接说明

更新时间：2026-04-26  
项目根目录：`C:\Users\24020\Desktop\Tg预测项目`

本文档用于把“共聚物 Tg 的统计预测方法研究——从虚拟数据增强到 DNA 跨域拓展”论文写作工作交接给其他 AI 或后续写作者。重点说明这两天完成的工作、当前产物、相关脚本、数据与模型结果来源，以及下一步应如何继续。

## 1. 当前任务背景

参赛论文题目为：

```text
共聚物 Tg 的统计预测方法研究——从虚拟数据增强到 DNA 跨域拓展
```

目标是写成全国大学生统计建模大赛论文 Word 完整版，并严格对齐比赛须知：

```text
C:\Users\24020\Desktop\111\比赛论文\20260313135711111607_附件4-大赛须知.docx
```

用户明确要求：

- 不要把论文写成工程中期报告。
- 不要在正文中过度暴露脚本名、内部实验目录名、具体模型代号。
- 不要自己发挥版式，未知格式要照搬附件4模板。
- 附件4第13页是封面模板，需去掉红色说明标注，只保留正式黑色封面内容。
- 论文要体现项目完整工作量，但表达要正式、自洽、像参赛论文。
- 图表要正式，不要“路边一条”的草图。

## 2. 当前 Word/PDF 产物

当前已生成的 Word 与 PDF 在：

```text
output/doc/作品全文-组别-待填作品编号.docx
output/doc/paper_full_draft.docx
output/doc/paper_full_draft.pdf
```

说明：

- `作品全文-组别-待填作品编号.docx` 是按比赛命名格式准备的 Word 完整版草案。
- `paper_full_draft.docx` 是同内容的 ASCII 文件名版本，便于脚本和 LibreOffice 处理。
- `paper_full_draft.pdf` 是用 LibreOffice 从 Word 转出的渲染检查 PDF。
- 当前 PDF 渲染为 A4，约 14 页，文件大小约 0.5 MB，远小于 100 MB。
- 封面中的作品编号、学校、参赛队员、指导老师仍是“待填”，需要用户最终手动填写。
- PDF 匿名版尚未单独生成。提交匿名版时应另存一份，删除封面页和致谢页，并确认正文中无学校、队员、指导老师信息。

当前版本已经修过两处用户明确指出的问题：

- 图1右下角错误斜箭头已修正为水平连接箭头。
- 封面已改为附件4第13页风格：作品编号、赛事标题、参赛作品、参赛学校、论文题目、参赛队员、指导老师，下划线字段，红色说明标注不进入正式稿。

但当前版本仍应视为“可继续精修的草案”，不是用户完全满意的最终稿。

## 3. Word 生成脚本

当前 Word/PDF 的生成入口：

```text
scripts/build_competition_paper_docx.py
```

脚本功能：

- 用 `python-docx` 创建 Word 完整版。
- 生成 3 张论文插图。
- 写入封面、摘要、目录、表格与插图清单、正文、参考文献、附录、致谢。
- 设置 A4 页面和比赛默认页边距：上/下 2.54 cm，左/右 3.17 cm。
- 正文页码从正文第一页重新开始。
- 输出 Word 到 `output/doc/`。

生成命令：

```powershell
python scripts\build_competition_paper_docx.py
Copy-Item -LiteralPath output\doc\paper_full_draft.docx -Destination "output\doc\作品全文-组别-待填作品编号.docx" -Force
```

用 LibreOffice 转 PDF 检查：

```powershell
$soffice="C:\Program Files\LibreOffice\program\soffice.exe"
$in=(Resolve-Path output\doc\paper_full_draft.docx).Path
$out=(Resolve-Path output\doc).Path
$profile=(Resolve-Path tmp).Path + "\lo_profile_" + [guid]::NewGuid().ToString()
& $soffice "-env:UserInstallation=file:///$($profile -replace '\\','/')" --headless --convert-to pdf --outdir $out $in
```

渲染 PDF 为图片检查：

```powershell
Remove-Item -Path tmp\doc_render\paper_page-*.png -Force -ErrorAction SilentlyContinue
pdftoppm -png -f 1 -l 8 output\doc\paper_full_draft.pdf tmp\doc_render\paper_page
```

渲染图片位置：

```text
tmp/doc_render/paper_page-01.png
tmp/doc_render/paper_page-02.png
...
```

## 4. 当前插图文件

插图由 `scripts/build_competition_paper_docx.py` 自动生成，输出到：

```text
output/doc/figures/figure1_framework.png
output/doc/figures/figure2_model.png
output/doc/figures/figure3_metrics.png
```

当前三张图含义：

- `figure1_framework.png`：从均聚物端点学习、虚拟共聚物弱标签、真实共聚物清洗分组、DNA 跨域验证到统一预测系统的技术路线。
- `figure2_model.png`：物理先验门控残差模型示意图，表达“预测 Tg = 物理基线 + 受约束残差”。
- `figure3_metrics.png`：当前严格评估下均聚物、一般共聚物、DNA 相关共聚物的 R² 和 MAE。

用户对图的审美要求很高。当前图比第一版有所改进，但仍可能不够正式。下一位 AI 如继续修改，建议：

- 不要用过于简陋的方框流程图。
- 可以改为更像论文图的矢量风格：统一线宽、统一字号、减少空白、提高信息密度。
- 图中文字必须清楚、无重叠，图题放在图下方。
- 插图最好直接反映论文主线，而不是工程模块图。

## 5. 比赛格式要求来源

比赛须知原文件：

```text
C:\Users\24020\Desktop\111\比赛论文\20260313135711111607_附件4-大赛须知.docx
```

为了避免 Python 读取中文路径出错，已经复制过一个 ASCII 路径版本：

```text
tmp/docs/attachment4.docx
```

附件4被 LibreOffice 转成 PDF 后的渲染文件：

```text
tmp/template_render/attachment4.pdf
```

附件4第13页渲染图片：

```text
tmp/template_render/attachment_page-13.png
```

第13页是封面模板，含红色虚线说明标注。正式论文中应照搬黑色部分结构，清除红色标注。页面说明包括：

- 作品编号：黑体，三号。
- 封面主标题：方正小标宋简体，二号；行距固定值 45 磅。
- “参赛作品”：方正小标宋简体，一号；行距固定值 45 磅。
- 页边距：上下 2.54 cm，左右 3.17 cm。
- 参赛学校、论文题目、参赛队员、指导老师用下划线字段。

附件4正文格式要求包括：

- Word 完整版包含八部分：封面页、摘要与关键词、章节目录、表格与插图清单、论文正文、参考文献、附录、致谢。
- PDF 匿名版在 Word 完整版基础上去掉封面页和致谢页，其余六部分完全一致。
- 除封面页、致谢外，不得出现学校、参赛队及指导教师信息。
- 正文总标题：方正小标宋简体三号。
- 一级标题：黑体小三。
- 二级标题：楷体四号。
- 三级标题：宋体小四加粗。
- 正文：宋体小四。
- 摘要、目录、参考文献等名称：黑体四号。
- 关键词名称：黑体小四。
- 表题在表上方，图题在图下方，图表连续编号。
- 除表格内容外，其余部分固定 24 磅行距，段前段后 0。

## 6. 论文草稿历史文件

前序论文写作过程中生成了多个 Markdown 草稿，均在：

```text
docs/research/
```

主要文件：

```text
docs/research/比赛论文写作方案-共聚物Tg统计预测-2026-04-26.md
docs/research/比赛论文写作自审记录-2026-04-26.md
docs/research/比赛论文正文初稿-共聚物Tg统计预测-2026-04-26.md
docs/research/比赛论文正文扩展稿-NatureStyle-共聚物Tg统计预测-2026-04-26.md
docs/research/比赛论文正文重构稿-共聚物Tg统计预测-2026-04-26.md
```

用途说明：

- `比赛论文写作方案...`：最早的论文组织方案，梳理如何扣题。
- `比赛论文正文初稿...`：第一版中文正文，偏简单。
- `比赛论文正文扩展稿-NatureStyle...`：按 Nature 风格扩展，内容更丰富，但组织不符合比赛论文格式。
- `比赛论文正文重构稿...`：较完整地讲清楚“均聚物源模型 - 虚拟数据 - 真实共聚物 - DNA 跨域 - 路由应用”的故事，是当前 Word 内容的重要素材来源。
- `比赛论文写作自审记录...`：对论文结构、扣题和格式的自审记录。

注意：这些 Markdown 是素材，不是最终格式。下一位 AI 应优先使用它们补充内容逻辑，而不是直接复制为最终论文。

## 7. 这两天完成的算法与实验工作概览

论文写作依托的是前两天围绕“全能 Tg 模型”和共聚物预测完成的一批实验与脚本。核心目标是建立一个尽可能通用的 Tg 预测体系，覆盖：

- 均聚物。
- 一般共聚物。
- 核酸/DNA 相关共聚物。
- 虚拟共聚物弱标签增强。

### 7.1 虚拟共聚物数据生成

关键脚本：

```text
scripts/generate_virtual_copolymer_dataset.py
```

完成的主要改造：

- 支持二元、多元、random、block、both 等共聚物生成。
- 支持从内部端点库自动生成，也支持 CSV recipe 输入。
- 修复早期“每预测一条就重新加载模型”的问题，改为一个任务中只加载一次预测器。
- 支持端点缓存，重复端点不重复 featurize。
- 支持 `--resume` 断点续跑。
- 支持优先复用 7k 去重均聚物已有端点结果，未命中时再调用均聚物源模型预测。

论文中应这样表达：

> 虚拟数据由端点 Tg 估计、Fox 混合规律、组成权重和链段架构近似生成，是低保真弱标签，不是实验真值。虚拟数据主要用于弱监督增强和分布偏移诊断，不能无约束加入最终训练。

不要在正文中写太多脚本名或命令细节。

### 7.2 真实共聚物数据清洗

关键脚本：

```text
scripts/parse_polyinfo_raw.py
scripts/filter_polyinfo_copolymer_conflicts.py
```

完成内容：

- 从原始复制粘贴的 PolyInfo 共聚物记录中抽取表格。
- 做端点映射、比例方向审计、单位处理。
- 剔除 pure endpoint rows，避免端点泄漏。
- 对同一体系同一组成下 Tg 冲突过大的记录进行过滤。
- 形成清洗后的真实共聚物数据，用于 group holdout。

论文中应强调：

- 共聚物数据清洗本身是统计建模的一部分。
- 随机划分会高估性能，体系级 group holdout 更能反映新共聚物体系外推能力。

### 7.3 统一单一回归模型

关键脚本：

```text
scripts/train_universal_tg_single_regressor.py
src/ml/universal_tg_model.py
```

相关实验输出：

```text
results/universal_single_regressor/
results/universal_single_regressor/scoreboard.json
results/universal_single_regressor/exp56_homo_local_fox_pred_delta_nonhomo_cal_lowfox_shrink_nopure/
results/universal_single_regressor/exp57_filtered_virtual_polyinfo_near400_w005/
results/universal_single_regressor/exp58_virtual_teacher_consistent200_w002/
```

当前论文主结果采用的模型逻辑：

- 使用均聚物端点学习作为共聚物预测的物理先验来源。
- 共聚物层面使用端点 Tg、Fox 基线、组成特征和结构表示。
- 训练一个单一回归模型，而不是完全分开的多个任务模型。
- 用样本权重缓解均聚物大样本压制共聚物小样本。
- 以三任务最小 R² 作为主指标，避免只优化一个任务。

当前主模型结果：

```text
均聚物 random holdout: R2=0.887, MAE=27.16
一般共聚物 PolyInfo group holdout: R2=0.849, MAE=16.65
DNA/核酸相关共聚物 group holdout: R2=0.817, MAE=6.27
综合 min-R2: 0.817
```

虚拟数据实验结论：

```text
不直接使用虚拟样本：PolyInfo group R2=0.849, DNA group R2=0.817
加入结构近邻虚拟样本：PolyInfo group R2=0.827, DNA group R2=0.858
加入 teacher-consistency 过滤虚拟样本：PolyInfo group R2≈0.850, DNA group R2=0.817
```

论文中推荐表达：

> 虚拟数据能提升 DNA 相关任务，但可能损害一般共聚物外推。说明虚拟增强不是越多越好，而需要端点可靠性、物理一致性和真实分布相似性共同约束。

### 7.4 统一预测路由系统

关键脚本：

```text
scripts/predict_polymer_tg_universal.py
```

这个脚本用于实际应用预测，能根据输入证据选择路线：

- 单一 repeat-unit：均聚物预测。
- 未知二元 random 共聚物：端点/Fox 外推与受约束残差。
- 已知体系：同体系局部残差插值。
- 多元 random 共聚物：多端点 Fox 近似。
- block 共聚物：输出近似和 Tg window。
- 端点缺失：使用端点估计 fallback，并降低置信度。
- DNA 相关体系：若端点可靠，可走 DNA 相关应用路线。

用户之前要求论文中体现“路由模型/专用模型/统一脚本”的工作量，但不能写得像脚本说明书。建议正文中放在“统一预测系统与应用边界”，用方法学语言描述：

> 在严格评估模型之外，本文构建证据自适应预测系统，将不同输入证据条件下的预测任务分发到相应的可解释路线，并在输出中记录预测依据和风险边界。

具体脚本名可放在附录或代码说明，不建议放正文。

## 8. 长时间自动迭代与实验记录

这两天还搭建过一个本地控制、服务器跑实验的长期 agent loop。

相关脚本：

```text
scripts/codex_universal_tg_agent_loop.py
scripts/codex_universal_tg_agent_loop.ps1
scripts/codex_universal_tg_agent_loop.sh
scripts/remote_tg_command.py
scripts/run_universal_tg_iteration_queue.py
```

相关文档与日志：

```text
AGENTS.md
docs/research/universal-tg-task-queue.md
docs/research/universal-tg-iteration-log.md
docs/research/universal-tg-bottleneck-report-2026-04-26-round25.md
```

用途：

- `AGENTS.md` 定义了长期迭代协议、指标、停止条件、服务器目录规则。
- `codex_universal_tg_agent_loop.py` 用于本地多轮启动 Codex，让每轮重新读取任务队列和迭代日志。
- `remote_tg_command.py` 用于从本地通过 SSH/Paramiko 调服务器命令。
- `universal-tg-iteration-log.md` 是最重要的实验日志，记录多轮假设、命令、结果和结论。
- `universal-tg-bottleneck-report...` 总结了当前性能瓶颈。

论文写作时可参考这些日志，但不要把 agent loop 写进正文。它属于工程过程，不是论文主要方法。

## 9. 服务器与路径注意事项

远程服务器信息在历史对话中使用过：

```text
SSH: sheng-xiang@100.64.0.4
远程项目目录: ~/Tgprediction
远程 Python: /home/sheng-xiang/miniconda3/envs/llm4graphgen/bin/python
```

重要限制：

- 服务器上只能在 `~/Tgprediction` 内工作。
- 不要在仓库文件中写入密码。
- 如果本地需要用密码调用远程命令，应在 PowerShell 环境变量中设置：

```powershell
$env:TG_REMOTE_PASSWORD = "本地临时设置，不要写入文件"
```

本次论文 Word 生成主要在本地完成，不依赖服务器。

## 10. 当前论文内容组织

当前 Word 论文正文结构为：

```text
一、问题描述与研究思路
  （一）研究背景
  （二）研究目标与技术路线

二、数据来源与质量控制
  （一）数据来源
  （二）真实共聚物数据质量控制
  （三）虚拟共聚物数据生成原则

三、指标选择与评价设计

四、模型建立
  （一）端点物理先验模型
  （二）物理先验门控残差模型

五、求解、检验与结果分析
  （一）主模型性能
  （二）虚拟数据增强的收益与风险
  （三）误差来源分析

六、统一预测系统与应用边界

七、结论与建议
  （一）主要结论
  （二）进一步建议
```

这个结构符合附件4对正文应包含“问题描述、指标选择、数据描述、模型建立、求解和检验、模型结果分析解释、结论建议”的要求。

## 11. 当前版本的主要不足

用户对论文质量要求较高，当前版本仍有以下不足，下一位 AI 应优先处理：

1. 图表还可以继续美化。
   当前图1已修正箭头，但仍偏流程图风格，建议改成更正式的论文插图。

2. 封面虽然按第13页结构修正，但标题较长，当前在封面中会换行。换行本身可以接受，但应人工检查下划线和字段位置是否足够接近模板。

3. 目录是手写目录，不是 Word 自动目录。
   如果最终提交要求自动页码，建议在 Word/LibreOffice 中更新目录域，或者手工补充页码。

4. 正文内容比最早版本正式，但仍偏短。
   当前 Word 约 14 页，正文约 6500 字。若希望更有“工作量”，可从 `docs/research/比赛论文正文重构稿...` 中补充：
   - 端点模型如何用于虚拟数据。
   - 真实共聚物数据冲突案例。
   - 为什么 group holdout 比 random holdout 更严格。
   - 虚拟数据增强为什么会造成分布偏移。
   - DNA 相关样本为何是跨域验证而非完整 DNA 材料覆盖。

5. 参考文献需进一步规范。
   当前参考文献是可读列表，但未完全按中文期刊或 GB/T 7714 格式整理。

6. 匿名 PDF 尚未制作。
   需要删除封面和致谢后再导出匿名版。

## 12. 建议下一步执行顺序

建议下一位 AI 按以下顺序接手：

1. 先打开并查看附件4第13页及正文模板页，确认封面和正文版式。

2. 打开当前 PDF：

```text
output/doc/paper_full_draft.pdf
```

逐页检查：

- 封面是否像附件4第13页。
- 摘要页是否符合“摘要/关键词”格式。
- 目录和图表清单是否需要页码。
- 正文标题字体是否一致。
- 表格是否跨页或拥挤。
- 图1、图2、图3是否足够正式。

3. 如需修改内容或格式，优先改：

```text
scripts/build_competition_paper_docx.py
```

然后重新生成 Word/PDF，不建议直接手工改 Word 后丢失可复现性。

4. 若要扩写正文，优先参考：

```text
docs/research/比赛论文正文重构稿-共聚物Tg统计预测-2026-04-26.md
docs/research/universal-tg-bottleneck-report-2026-04-26-round25.md
docs/research/universal-tg-iteration-log.md
```

5. 最后准备提交材料：

- Word 完整版：保留封面和致谢，填写真实队伍信息。
- PDF 匿名版：删除封面和致谢，不得出现学校、队员、指导老师信息。
- 数据及代码包：可整理 `scripts/`、`docs/research/`、`results/universal_single_regressor/` 中与论文直接相关部分。

## 13. 当前关键文件清单

论文生成与输出：

```text
scripts/build_competition_paper_docx.py
output/doc/作品全文-组别-待填作品编号.docx
output/doc/paper_full_draft.docx
output/doc/paper_full_draft.pdf
output/doc/比赛论文格式对齐检查表.md
output/doc/figures/figure1_framework.png
output/doc/figures/figure2_model.png
output/doc/figures/figure3_metrics.png
```

比赛要求与模板：

```text
C:\Users\24020\Desktop\111\比赛论文\20260313135711111607_附件4-大赛须知.docx
tmp/docs/attachment4.docx
tmp/template_render/attachment4.pdf
tmp/template_render/attachment_page-13.png
```

论文草稿：

```text
docs/research/比赛论文写作方案-共聚物Tg统计预测-2026-04-26.md
docs/research/比赛论文写作自审记录-2026-04-26.md
docs/research/比赛论文正文初稿-共聚物Tg统计预测-2026-04-26.md
docs/research/比赛论文正文扩展稿-NatureStyle-共聚物Tg统计预测-2026-04-26.md
docs/research/比赛论文正文重构稿-共聚物Tg统计预测-2026-04-26.md
```

核心建模脚本：

```text
scripts/generate_virtual_copolymer_dataset.py
scripts/parse_polyinfo_raw.py
scripts/filter_polyinfo_copolymer_conflicts.py
scripts/train_universal_tg_single_regressor.py
scripts/predict_polymer_tg_universal.py
src/ml/universal_tg_model.py
```

实验结果与日志：

```text
results/universal_single_regressor/scoreboard.json
results/universal_single_regressor/exp56_homo_local_fox_pred_delta_nonhomo_cal_lowfox_shrink_nopure/
results/universal_single_regressor/exp57_filtered_virtual_polyinfo_near400_w005/
results/universal_single_regressor/exp58_virtual_teacher_consistent200_w002/
docs/research/universal-tg-iteration-log.md
docs/research/universal-tg-task-queue.md
docs/research/universal-tg-bottleneck-report-2026-04-26-round25.md
```

长期迭代与远程控制：

```text
AGENTS.md
scripts/codex_universal_tg_agent_loop.py
scripts/codex_universal_tg_agent_loop.ps1
scripts/codex_universal_tg_agent_loop.sh
scripts/remote_tg_command.py
scripts/run_universal_tg_iteration_queue.py
```

## 14. 给下一位 AI 的写作原则

请按以下原则继续：

- 正文讲“统计建模方法”，不要讲“我写了哪个脚本”。
- 脚本名、实验目录名、内部模型代号放附录或代码说明，不放正文。
- 论文主线必须扣住标题：
  - 共聚物 Tg 统计预测。
  - 虚拟数据增强。
  - DNA 跨域拓展。
- 一定强调严格评估：
  - 一般共聚物使用体系留出。
  - DNA 相关共聚物使用分组留出。
  - 不用随机划分虚高结果冒充泛化。
- 不要声称已经达到 R²=0.95。
  当前可信结果是阶段性结果，优势在于非泄漏、可解释、链条完整。
- 图表要服务论文叙事，不要像工程流程截图。
- 格式以附件4为准，尤其封面、标题层级、字体字号、图表题位置和页边距。

